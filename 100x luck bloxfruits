--[[
    💥 FAKE LUCK HUB
    SOMENTE VISUAL
    Não altera Blox Fruits nem suas mecânicas.
]]

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

--==================================================
-- GUI
--==================================================

local gui = Instance.new("ScreenGui")
gui.Name = "FakeLuckHub"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

--==================================================
-- CORES
--==================================================

local COLORS = {
    Verde = Color3.fromRGB(55, 255, 100),
    Amarelo = Color3.fromRGB(255, 220, 45),
    Azul = Color3.fromRGB(65, 150, 255)
}

local currentColor = COLORS.Verde
local rgbEnabled = false
local rgbHue = 0

--==================================================
-- LOGO
--==================================================

local logo = Instance.new("TextButton")
logo.Size = UDim2.fromOffset(58, 58)
logo.Position = UDim2.new(0, 25, 0.5, -29)

logo.BackgroundColor3 = Color3.fromRGB(17,17,22)
logo.Text = "💥"
logo.TextSize = 30
logo.Font = Enum.Font.GothamBold
logo.AutoButtonColor = false
logo.Parent = gui

local logoCorner = Instance.new("UICorner")
logoCorner.CornerRadius = UDim.new(1,0)
logoCorner.Parent = logo

local logoStroke = Instance.new("UIStroke")
logoStroke.Thickness = 3
logoStroke.Color = currentColor
logoStroke.Parent = logo

--==================================================
-- MAIN FRAME
--==================================================

local main = Instance.new("Frame")
main.Size = UDim2.fromOffset(430, 330)
main.Position = UDim2.new(0.5, -215, 0.5, -165)

main.BackgroundColor3 = Color3.fromRGB(12,12,17)
main.Visible = false
main.Parent = gui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0,14)
mainCorner.Parent = main

local mainStroke = Instance.new("UIStroke")
mainStroke.Thickness = 2
mainStroke.Color = currentColor
mainStroke.Parent = main

--==================================================
-- SIDEBAR
--==================================================

local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0,115,1,0)
sidebar.BackgroundColor3 = Color3.fromRGB(18,18,24)
sidebar.BorderSizePixel = 0
sidebar.Parent = main

local sidebarCorner = Instance.new("UICorner")
sidebarCorner.CornerRadius = UDim.new(0,14)
sidebarCorner.Parent = sidebar

--==================================================
-- TITLE
--==================================================

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,-20,0,45)
title.Position = UDim2.fromOffset(10,5)

title.BackgroundTransparency = 1
title.Text = "💥 HUB"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.TextSize = 19
title.Font = Enum.Font.GothamBold

title.Parent = sidebar

--==================================================
-- CATEGORY BUTTON
--==================================================

local function categoryButton(text, y)

    local button = Instance.new("TextButton")

    button.Size = UDim2.new(1,-16,0,45)
    button.Position = UDim2.new(0,8,0,y)

    button.BackgroundColor3 = Color3.fromRGB(27,27,34)
    button.Text = text

    button.TextColor3 = Color3.fromRGB(210,210,215)
    button.TextSize = 14
    button.Font = Enum.Font.GothamBold

    button.AutoButtonColor = false
    button.Parent = sidebar

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0,8)
    corner.Parent = button

    return button
end

local luckTab = categoryButton("LUCK",60)
local configTab = categoryButton("CONFIG",112)

--==================================================
-- CONTENT CONTAINER
--==================================================

local content = Instance.new("Frame")
content.Size = UDim2.new(1,-125,1,-10)
content.Position = UDim2.new(0,120,0,5)

content.BackgroundTransparency = 1
content.Parent = main

--==================================================
-- PAGE CREATOR
--==================================================

local function createPage()

    local page = Instance.new("ScrollingFrame")

    page.Size = UDim2.fromScale(1,1)

    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0

    page.ScrollBarThickness = 5
    page.ScrollBarImageColor3 = currentColor

    page.CanvasSize = UDim2.new(0,0,0,0)

    page.ScrollingDirection = Enum.ScrollingDirection.Y

    page.Visible = false
    page.Parent = content

    local layout = Instance.new("UIListLayout")

    layout.Padding = UDim.new(0,10)
    layout.SortOrder = Enum.SortOrder.LayoutOrder

    layout.Parent = page

    local padding = Instance.new("UIPadding")

    padding.PaddingTop = UDim.new(0,8)
    padding.PaddingBottom = UDim.new(0,12)
    padding.PaddingLeft = UDim.new(0,8)
    padding.PaddingRight = UDim.new(0,8)

    padding.Parent = page

    layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()

        page.CanvasSize = UDim2.new(
            0,
            0,
            0,
            layout.AbsoluteContentSize.Y + 20
        )

    end)

    return page
end

local luckPage = createPage()
local configPage = createPage()

--==================================================
-- PAGE TITLE
--==================================================

local function pageTitle(parent,text)

    local label = Instance.new("TextLabel")

    label.Size = UDim2.new(1,0,0,38)

    label.BackgroundTransparency = 1

    label.Text = text
    label.TextColor3 = currentColor

    label.TextSize = 20
    label.Font = Enum.Font.GothamBold

    label.TextXAlignment = Enum.TextXAlignment.Left

    label.LayoutOrder = 1
    label.Parent = parent

    return label
end

local luckTitle = pageTitle(luckPage,"Luck")
local configTitle = pageTitle(configPage,"Configuração")

--==================================================
-- BUTTON
--==================================================

local function createAction(parent,text,callback)

    local button = Instance.new("TextButton")

    button.Size = UDim2.new(1,-5,0,45)

    button.BackgroundColor3 = Color3.fromRGB(26,26,33)

    button.Text = text
    button.TextColor3 = Color3.fromRGB(240,240,240)

    button.TextSize = 15
    button.Font = Enum.Font.GothamBold

    button.AutoButtonColor = false

    button.LayoutOrder = 2
    button.Parent = parent

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0,9)
    corner.Parent = button

    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 1
    stroke.Color = Color3.fromRGB(55,55,65)
    stroke.Parent = button

    button.MouseButton1Click:Connect(callback)

    return button
end

--==================================================
-- STATUS
--==================================================

local status = Instance.new("TextLabel")

status.Size = UDim2.new(1,-5,0,35)

status.BackgroundTransparency = 1

status.Text = "Current Luck: 1x"
status.TextColor3 = currentColor

status.TextSize = 14
status.Font = Enum.Font.Gotham

status.TextXAlignment = Enum.TextXAlignment.Left

status.LayoutOrder = 2
status.Parent = luckPage

--==================================================
-- LUCK FUNCTIONS
--==================================================

createAction(luckPage,"10x Luck",function()

    status.Text = "Current Luck: 10x"

end)

createAction(luckPage,"100x Luck",function()

    status.Text = "Current Luck: 100x"

end)

createAction(luckPage,"1000x Luck",function()

    status.Text = "Current Luck: 1000x"

end)

--==================================================
-- CONFIG FUNCTIONS
--==================================================

createAction(configPage,"🟢 Verde",function()

    rgbEnabled = false
    currentColor = COLORS.Verde

end)

createAction(configPage,"🟡 Amarelo",function()

    rgbEnabled = false
    currentColor = COLORS.Amarelo

end)

createAction(configPage,"🔵 Azul",function()

    rgbEnabled = false
    currentColor = COLORS.Azul

end)

local rgbButton

rgbButton = createAction(configPage,"🌈 RGB: OFF",function()

    rgbEnabled = not rgbEnabled

    if rgbEnabled then
        rgbButton.Text = "🌈 RGB: ON"
    else
        rgbButton.Text = "🌈 RGB: OFF"
    end

end)

--==================================================
-- PAGE SWITCH
--==================================================

local function showPage(page)

    luckPage.Visible = false
    configPage.Visible = false

    page.Visible = true

end

showPage(luckPage)

luckTab.MouseButton1Click:Connect(function()

    showPage(luckPage)

end)

configTab.MouseButton1Click:Connect(function()

    showPage(configPage)

end)

--==================================================
-- RGB EFFECT
--==================================================

RunService.RenderStepped:Connect(function(dt)

    if rgbEnabled then

        rgbHue = (rgbHue + dt * 0.25) % 1

        currentColor = Color3.fromHSV(rgbHue,1,1)

    end

    logoStroke.Color = currentColor
    mainStroke.Color = currentColor

    luckTitle.TextColor3 = currentColor
    configTitle.TextColor3 = currentColor

    pageTitle(luckPage,"Luck")

    luckPage.ScrollBarImageColor3 = currentColor
    configPage.ScrollBarImageColor3 = currentColor

end)

--==================================================
-- ABRIR / FECHAR
--==================================================

logo.MouseButton1Click:Connect(function()

    main.Visible = not main.Visible

end)

--==================================================
-- DRAG SYSTEM
--==================================================
-- Sistema refeito para usar o deslocamento absoluto
-- do input e evitar o "pulo" inicial.

local function makeDraggable(object, handle)

    local dragging = false
    local dragInput

    local startInputPosition
    local startObjectPosition

    handle.InputBegan:Connect(function(input)

        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then

            dragging = true

            startInputPosition = input.Position
            startObjectPosition = object.Position

            input.Changed:Connect(function()

                if input.UserInputState == Enum.UserInputState.End then

                    dragging = false

                end

            end)

        end

    end)

    handle.InputChanged:Connect(function(input)

        if input.UserInputType == Enum.UserInputType.MouseMovement
        or input.UserInputType == Enum.UserInputType.Touch then

            dragInput = input

        end

    end)

    UIS.InputChanged:Connect(function(input)

        if input ~= dragInput or not dragging then
            return
        end

        local delta = input.Position - startInputPosition

        object.Position = UDim2.new(

            startObjectPosition.X.Scale,
            startObjectPosition.X.Offset + delta.X,

            startObjectPosition.Y.Scale,
            startObjectPosition.Y.Offset + delta.Y

        )

    end)

end

-- Logo pode ser movida segurando nela
makeDraggable(logo,logo)

-- Menu pode ser movido segurando o título da sidebar
makeDraggable(main,sidebar)

--==================================================
-- APLICAÇÃO DA COR NOS BOTÕES
--==================================================

RunService.RenderStepped:Connect(function()

    for _,obj in ipairs(sidebar:GetChildren()) do

        if obj:IsA("TextButton") then

            if obj == luckTab or obj == configTab then

                obj.TextColor3 = Color3.fromRGB(220,220,225)

            end

        end

    end

end)
